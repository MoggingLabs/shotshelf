{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the Shotshelf shelf window. Every entry is local-device only — nothing here can reach the network. Clipboard read/monitor catch Win+Shift+S / cmd-ctrl-shift-4 captures (phase 02); clipboard write backs the copy fallback for drag-out (phase 04).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "fs:default",
    "clipboard:read-all",
    "clipboard:monitor-all",
    "clipboard:write-all",
    "drag:default"
  ]
}
