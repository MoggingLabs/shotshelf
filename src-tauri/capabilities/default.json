{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the shelf window itself may do. Capabilities gate the webview — the least-trusted process here — and nothing else: work done in Rust with an AppHandle is not covered by this file, so a permission listed for the benefit of Rust code grants reach to the webview and buys nothing. Every entry below has a caller in src/, and that is the whole rule.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "drag:default"
  ]
}
